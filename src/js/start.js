/**
 * @badeggg
 * 2016.3.7 start coding
 * 开始
 */
require(['slide', 'magnifier', 'result', 'teach'], function (slide, magnifier, result, teach) {
  slide();
  magnifier();
  result();
  teach();
});
